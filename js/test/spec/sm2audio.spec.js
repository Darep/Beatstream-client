define([
    'beatstream/audio-modules/sm2audio'
], function(SM2Audio) {
    describe('SM2 Audio', function () {

        // SUT
        var audio;

        beforeEach(function() {
            audio = new SM2Audio();
        });

        xit('should play song through', function () {
            throw('not done');
        });
    });
});
