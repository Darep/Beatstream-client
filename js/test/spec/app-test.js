define(['beatstream/app'], function(app) {
    describe('App', function () {
        var fixture;

        beforeEach(function () {
        });

        it('initializes the API with correct URL', function () {
            console.log(app);
        });
    });
});
