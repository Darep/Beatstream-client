define([],
function () {
    var SettingsDialog = (function () {
        this.el = $('#dialog-settings');

        return {
            show: function () {

            }
        };
    }());

    return SettingsDialog;
});
